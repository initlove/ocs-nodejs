1. define the root user
2. define something like the port, the hostname?
3. add the icons, make the stock server
    the icons were collected from most rpms
   so in the compose server, we will check if the icons were existed.
    thus make compose process faster.
